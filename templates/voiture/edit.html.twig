{% extends 'base.html.twig' %}

{% block title %}Edit Voiture{% endblock %}

{% block body %}
    <div class="mt-5"><br></div>
    <div class="mt-2"><br></div>
    <h1>Edit Voiture {{voiture.nom}}</h1>

    {{ include('voiture/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('voiture_index') }}">back to list</a>

    {{ include('voiture/_delete_form.html.twig') }}
{% endblock %}
